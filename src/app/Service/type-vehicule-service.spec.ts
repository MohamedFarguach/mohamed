import { TypeVehiculeService } from './type-vehicule-service';

describe('TypeVehiculeService', () => {
  it('should create an instance', () => {
    expect(new TypeVehiculeService()).toBeTruthy();
  });
});
