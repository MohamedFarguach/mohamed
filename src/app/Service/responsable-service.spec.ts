import { ResponsableService } from './responsable-service';

describe('ResponsableService', () => {
  it('should create an instance', () => {
    expect(new ResponsableService()).toBeTruthy();
  });
});
